/**
 * Create Admin User
 *
 * Admin accounts can no longer be created via the public signup form
 * (for security). Use this artisan command instead, run from the
 * travix-api folder:
 *
 *   php artisan admin:create admin@travix.com yourpassword "Admin User"
 *
 * This will create a new admin account, or promote an existing
 * user (by email) to admin if one already exists.
 */
console.log('See comments in this file — admin accounts are created via artisan command, not JS.');
